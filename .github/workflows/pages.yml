name: Deploy PHOENIX

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout PHOENIX
        uses: actions/checkout@v4

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Prepare PHOENIX Frontend
        run: |
          mkdir -p _site
          cp -r frontend/* _site/

      - name: Upload PHOENIX Frontend
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

      - name: Deploy PHOENIX
        id: deployment
        uses: actions/deploy-pages@v4
